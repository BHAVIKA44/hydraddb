{{- if and .Values.tls.certManager.enabled .Values.tls.public.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "hydradb.fullname" . }}-server
  labels:
    {{- include "hydradb.labels" . | nindent 4 }}
spec:
  secretName: {{ include "hydradb.publicTlsSecretName" . }}
  duration: {{ .Values.tls.certManager.publicDuration }}
  renewBefore: {{ .Values.tls.certManager.publicRenewBefore }}
  privateKey:
    algorithm: ECDSA
    size: 256
    rotationPolicy: Always
  usages:
    - server auth
  dnsNames:
    - {{ include "hydradb.fullname" . }}-bolt.{{ .Release.Namespace }}.svc
    - {{ include "hydradb.fullname" . }}-bolt.{{ .Release.Namespace }}.svc.cluster.local
    - {{ include "hydradb.fullname" . }}-https.{{ .Release.Namespace }}.svc
    - {{ include "hydradb.fullname" . }}-https.{{ .Release.Namespace }}.svc.cluster.local
    - {{ printf "*.%s-node-headless.%s.svc" (include "hydradb.fullname" .) .Release.Namespace | quote }}
    - {{ printf "*.%s-node-headless.%s.svc.cluster.local" (include "hydradb.fullname" .) .Release.Namespace | quote }}
    {{- range .Values.tls.public.additionalDnsNames }}
    - {{ . | quote }}
    {{- end }}
  issuerRef:
    group: {{ .Values.tls.certManager.publicIssuerRef.group }}
    kind: {{ .Values.tls.certManager.publicIssuerRef.kind }}
    name: {{ required "tls.certManager.publicIssuerRef.name is required" .Values.tls.certManager.publicIssuerRef.name }}
{{- end }}
